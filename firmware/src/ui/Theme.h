#pragma once
#include <Arduino.h>
#include <Arduino_GFX_Library.h>

static const uint16_t UI_BG = RGB565(0,0,0);
static const uint16_t UI_WHITE = RGB565(255,255,255);
static const uint16_t UI_MUTED = RGB565(120,120,120);
static const uint16_t UI_BORDER = RGB565(60,60,60);
static const uint16_t UI_GREEN = RGB565(105,215,145);
static const uint16_t UI_BLUE = RGB565(120,170,240);
static const uint16_t UI_WARN = RGB565(220,180,80);
